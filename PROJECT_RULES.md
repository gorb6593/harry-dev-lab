# PROJECT_RULES.md

## 목적
- 이 문서는 `/Users/kiwoong/Downloads/rab` 학습 프로젝트의 공통 실행 규칙이다.
- 현재 학습 시작 지점은 `jpaLevel1`이다.
- 목표는 `5~7년차 백엔드 개발자`가 실무 경험은 있으나 기초를 다시 단단히 점검하고, JPA를 포함한 백엔드 핵심 개념을 코드로 다시 검증하는 것이다.
- 학습은 `질문 -> 생각 -> 코드 작성 -> 실행 결과 확인 -> 정리` 순서로 진행한다.
- 테스트 코드는 작성하지 않는다. 대신 실행 가능한 API, 콘솔 로그, SQL 로그, DB 상태 확인으로 검증한다.

## 기준 기술 스택
- Java는 `17`을 기준으로 한다.
- Spring Boot는 `3.x`를 기준으로 한다.
- JPA 학습은 Hibernate/Jakarta Persistence 기반으로 진행한다.

## 현재 저장소에 대한 해석 규칙
- 저장소에 이미 `Java 21`, `Spring Boot 4` 관련 설정이나 문서가 있더라도, `jpaLevel1` 학습 트랙에서는 `Java 17`, `Spring Boot 3.x` 기준을 우선 적용한다.
- 버전 차이로 인해 기존 문서와 충돌하는 부분이 있으면, 현재 학습 기준과 차이를 문서에 짧게 메모한 뒤 진행한다.
- 학습 중 버전 업그레이드는 하지 않는다. 기초 학습이 끝난 뒤 별도 단계로 다룬다.

## 학습 운영 원칙
- 단순히 기능 구현을 하지 않고, 항상 "왜 이렇게 동작하는가"를 설명할 수 있어야 한다.
- 각 단계는 작은 질문 하나를 해결하는 방식으로 진행한다.
- 한 번에 여러 개념을 섞지 않는다.
- 코드는 학습용이라도 실무 코드처럼 작성한다.
- 애매한 개념은 추측으로 넘기지 말고, 코드와 로그로 확인한다.
- DTO, Entity, Service, Repository, Controller 역할을 섞지 않는다.
- 학습 기록은 결과만 적지 않고, 틀린 가설과 수정된 이해도 같이 남긴다.

## 질문 기반 진행 방식
- 매 단계는 아래 형식을 기본으로 한다.

1. 이번 질문 정의
2. 내가 예상한 답변 작성
3. 이를 검증할 최소 코드 작성
4. API 호출 또는 실행 결과 확인
5. SQL/영속성 컨텍스트/트랜잭션 관점 해석
6. 한 줄 결론 정리

- 질문은 반드시 하나의 핵심 개념만 검증해야 한다.
- 질문 예시는 아래와 같다.
  - `save()`를 호출하면 언제 insert SQL이 나가는가?
  - 영속 상태와 준영속 상태의 차이는 코드에서 어떻게 드러나는가?
  - dirty checking은 정확히 어느 시점에 반영되는가?
  - `@Transactional`이 없으면 어디서 깨지는가?
  - 지연 로딩은 언제 실제 쿼리를 발생시키는가?

## 코드 작성 원칙
- 패키지는 역할 중심으로 분리한다.
- 추천 기본 구조는 아래와 같다.
  - `api`
  - `application`
  - `domain`
  - `infra`
- Controller는 요청/응답 처리와 흐름 진입만 담당한다.
- Service는 유스케이스 흐름과 트랜잭션 경계를 담당한다.
- Entity는 JPA 매핑과 도메인 상태 변경 책임에 집중한다.
- Repository는 조회/저장에 집중한다.
- 한 메서드에서 너무 많은 개념을 동시에 검증하지 않는다.
- 학습 단계에서는 Lombok 사용 여부를 일관되게 유지한다.
- Entity 연관관계는 처음부터 복잡하게 열지 않는다. 단방향부터 시작하고, 필요할 때만 양방향으로 확장한다.
- API 요청/응답 필드는 `camel_case`를 사용한다.

## 학습 검증 원칙
- 테스트 코드는 작성하지 않는다.
- 대신 아래 방식으로 검증한다.
  - API 호출 결과 확인
  - Hibernate SQL 로그 확인
  - 파라미터 바인딩 로그 확인
  - DB 직접 조회
  - 트랜잭션 전후 상태 비교
- 검증 불가한 코드는 학습 코드로 간주하지 않는다.
- "동작했다"로 끝내지 말고, 왜 그런 SQL이 나왔는지 설명 가능해야 한다.

## 문서화 원칙
- 각 학습 단계는 코드와 함께 문서도 남긴다.
- 문서에는 아래 5가지를 짧게 정리한다.
  - 질문
  - 예상
  - 코드 포인트
  - 실행 결과
  - 결론
- 문서는 길게 쓰기보다 다음 단계로 다시 읽기 쉽게 작성한다.
- 개념 설명은 복붙식 요약보다, 이번 코드에서 확인한 사실 중심으로 적는다.

## 전체 학습 플랜

### 0단계. 사전 점검
- Java 17, Gradle, Spring Boot 3.x 환경을 맞춘다.
- 프로젝트 실행이 되는지 확인한다.
- API/패키지/문서 규칙을 먼저 고정한다.
- JPA 로그 설정을 켠다.
- 목표: 학습 환경이 흔들리지 않도록 바닥을 고정한다.

### 1단계. JPA 시작 전 백엔드 기초 재점검
- 객체와 DB는 왜 다른지 설명할 수 있어야 한다.
- Layered Architecture를 다시 점검한다.
- DI, Bean, Proxy, AOP, Transaction Proxy 개념을 짧게 다시 확인한다.
- `@Transactional`이 어디에 붙어야 하는지 기준을 정리한다.
- 목표: JPA 이전에 Spring 동작 기반을 다시 분명히 한다.

### 2단계. JPA 기초 1 - 엔티티와 영속성 컨텍스트
- Entity와 DTO 차이를 명확히 구분한다.
- 영속성 컨텍스트, 1차 캐시, 쓰기 지연 저장소를 코드로 확인한다.
- `find`, `save`, `flush`, `clear`, `detach`를 직접 다뤄본다.
- insert SQL이 실제로 나가는 시점을 확인한다.
- 목표: JPA가 단순 ORM 라이브러리가 아니라 상태 관리 도구임을 이해한다.

### 3단계. JPA 기초 2 - 변경 감지와 트랜잭션
- dirty checking이 무엇인지 코드로 검증한다.
- 트랜잭션 범위 안/밖에서 엔티티가 어떻게 달라지는지 확인한다.
- merge를 늦게 다루고, 먼저 영속 상태 변경의 기본 흐름을 익힌다.
- 목표: update SQL을 직접 쓰지 않아도 되는 이유를 설명할 수 있게 한다.

### 4단계. JPA 기초 3 - 연관관계 매핑
- ManyToOne, OneToMany를 단방향부터 시작한다.
- 연관관계 주인 개념을 정확히 이해한다.
- mappedBy를 감으로 쓰지 않는다.
- 양방향 연관관계는 왜 관리 메서드가 필요한지 확인한다.
- 목표: 연관관계 매핑을 문법이 아니라 상태 일관성 관점에서 이해한다.

### 5단계. JPA 기초 4 - 조회 전략과 성능
- 즉시 로딩과 지연 로딩 차이를 확인한다.
- N+1 문제가 언제 생기는지 직접 본다.
- fetch join이 왜 필요한지 학습한다.
- 단순 조회는 Entity 반환과 DTO 조회를 구분해서 본다.
- 목표: "조회가 된다"와 "실무적으로 안전하다"를 구분한다.

### 6단계. JPA 기초 5 - 실무 설계 규칙
- Entity를 어디까지 노출하면 안 되는지 정리한다.
- setter 남발을 막고, 상태 변경 메서드 중심으로 구조를 잡는다.
- equals/hashCode, 기본 생성자, 컬렉션 초기화 등 실무 함정을 점검한다.
- 목표: 돌아가는 코드에서 끝나지 않고 유지보수 가능한 JPA 코드를 작성한다.

### 7단계. JPA 기초 6 - 트랜잭션과 동시성 연결
- JPA가 동시성 문제를 자동으로 해결해주지 않는다는 점을 확인한다.
- 낙관적 락과 비관적 락의 필요성을 이해한다.
- 먼저 `read -> modify -> write` 흐름을 코드로 만들고, 이후 락 전략으로 연결한다.
- 목표: JPA 학습을 실무 동시성 문제와 연결한다.

### 8단계. Querydsl/복잡한 조회로 확장
- JPA 기본 조회의 한계를 느낀 뒤 Querydsl로 넘어간다.
- 동적 조건, 페이징, 정렬, 복합 조회를 단계적으로 추가한다.
- 목표: JPA 기본기 위에 실무 조회 설계를 확장한다.

## 권장 질문 순서
1. Entity는 왜 필요하고 DTO와 왜 분리해야 하는가?
2. `save()` 직후 DB에 바로 insert 되는가?
3. 같은 트랜잭션에서 같은 엔티티를 두 번 조회하면 왜 같은 객체처럼 보이는가?
4. `@Transactional` 안에서 값만 바꿨는데 왜 update SQL이 나가는가?
5. `flush()`와 commit은 무엇이 다른가?
6. `clear()` 이후 다시 조회하면 왜 쿼리가 다시 나가는가?
7. 연관관계 주인은 왜 외래 키가 있는 쪽인가?
8. 지연 로딩은 언제 프록시를 초기화하는가?
9. N+1 문제는 왜 생기고 fetch join은 무엇을 바꾸는가?
10. JPA만으로 동시성 문제가 해결되지 않는 이유는 무엇인가?

## 금지 사항
- 처음부터 복잡한 도메인으로 들어가지 않는다.
- 연관관계를 여러 개 한 번에 열지 않는다.
- 개념 이해 없이 관성적으로 `cascade`, `fetch = EAGER`, `@Data`를 사용하지 않는다.
- Entity를 API 응답으로 바로 노출하지 않는다.
- 테스트 코드를 안 쓴다는 이유로 검증을 생략하지 않는다.
- "실무에서는 원래 이렇게 한다"라는 말만으로 결론 내리지 않는다.

## 완료 기준
- 각 단계마다 질문 하나 이상이 문서로 남아 있어야 한다.
- 각 질문은 반드시 실행 가능한 코드와 확인 결과가 있어야 한다.
- 각 질문에 대해 "왜 그런지"를 본인 언어로 3~5문장 안에 설명할 수 있어야 한다.
- 다음 단계로 넘어가기 전, 이전 단계 핵심 질문에 답할 수 있어야 한다.

## 이 문서를 사용하는 방법
- 학습을 시작할 때 먼저 현재 단계의 질문 1개를 고른다.
- 그 질문만 검증하는 최소 코드만 작성한다.
- 실행 후 문서에 결론을 남긴다.
- 그 다음 질문으로 넘어간다.
- 한 번에 많이 하지 않는다. 대신 끊어서 정확하게 간다.
