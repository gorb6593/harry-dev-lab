# PROJECT_RULES.md

## 목적
이 프로젝트는 학습용 토이 코드가 아니라, **실전 운영을 가정한 검증 프로젝트**다.
모든 산출물(코드/문서/테스트)은 시니어 개발자 기준의 품질로 작성한다.

## 핵심 원칙
1. 재현 가능성
- 주장은 반드시 재현 가능한 테스트/로그/지표로 증명한다.
- "잘 되는 것 같다"는 금지. 수치와 실패/성공 조건을 명시한다.

2. 가설 기반 진행
- 구현 전에 가설을 한 줄로 쓴다.
- 예시: "낙관적 락 적용 시 Lost Update는 사라지지만 재시도 비용이 증가한다."

3. 작은 변경, 빠른 검증
- 한 번에 하나의 원인만 바꾼다.
- 각 Step은 `실패 테스트 -> 해결 -> 통과 테스트`를 남긴다.

4. 운영 관점 우선
- 단일 인스턴스에서만 통하는 해법은 "제한된 해법"으로 표기한다.
- 멀티 인스턴스/장애/관측성 관점에서 반드시 재평가한다.

## 코드 규칙
1. 패키지 구조
- `domain`: 순수 도메인 모델
- `service`: 유스케이스/트랜잭션 경계
- `api`: HTTP 진입점 및 요청/응답 DTO
- `infra`: 저장소, Redis, 메시징, 외부 연동
- `testsupport`: 테스트 공통 유틸

2. 설계
- 서비스 메서드는 "무엇을 보장하는지"를 이름으로 드러낸다.
- 동시성 제어 위치를 명확히 한다(앱/DB/Redis 중 어디인지).
- 예외는 도메인 의미를 가지는 타입으로 변환해 던진다.

3. 금지
- 테스트 없이 동시성 관련 코드 머지 금지
- 근거 없는 `synchronized` 남발 금지
- 전역 mutable 상태 추가 금지(불가피하면 이유 문서화)

## 테스트 규칙
1. 테스트 계층
- 단위 테스트: 순수 로직 검증
- 통합 테스트: DB/Redis 포함 경계 검증
- 동시성 테스트: 스레드 경합 상황을 의도적으로 생성

2. 동시성 테스트 필수 항목
- 동시 요청 수
- 초기 값
- 기대 최종 값
- 실제 최종 값
- 실패/재시도 횟수
- 총 소요 시간

3. 테스트 안정성
- Flaky 테스트 금지
- 타임아웃/대기 조건은 명시적으로 관리
- 테스트가 느리면 원인을 문서화하고 분리 실행 전략 작성

## 문서 규칙
1. Step 문서 템플릿
- 문제 상황
- 가설
- 재현 방법
- 실패 증거(로그/스크린샷/수치)
- 해결 방법
- 트레이드오프
- 다음 액션

2. 근거 출처
- 기술 사실은 **공식 문서 우선**
- 블로그는 보조 설명으로 사용
- 출처 없는 주장 금지

3. 날짜 명시
- 버전/릴리즈/지원 범위는 문서 작성일 기준 날짜를 함께 남긴다.

## 성능/관측성 규칙
1. 최소 지표
- 처리량(ops/sec 또는 n requests/time)
- p95 응답 시간
- 오류율
- 정합성 위반 건수

2. 로그 규칙
- 요청 추적 ID 사용
- 동시성 실패 시 원인(락 충돌/타임아웃/낙관적락 충돌) 구분 로그

## 의사결정 규칙
1. ADR(Architecture Decision Record) 기록
- 중요한 선택은 `docs/adr-xxxx-*.md`로 기록
- 최소 포함: 배경, 선택지, 결정, 근거, 부작용

2. Done 기준(각 Step 공통)
- 실패 재현 테스트 존재
- 해결 후 통과 테스트 존재
- 트레이드오프 문서화
- 다음 Step 입력 조건 명확화

## 이 파일 사용 규칙
- 작업 시작 전 `PROJECT_RULES.md`를 먼저 읽는다.
- 규칙을 어길 경우, 이유와 보완 계획을 문서에 남긴다.
- 규칙이 현실과 맞지 않으면 합의 후 업데이트한다.
